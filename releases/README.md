# releases/

This directory will hold versioned release tarballs of the harness package,
generated via `npm pack` and committed only after explicit approval.

Individual `.tgz` and `.tar.gz` files in this directory are excluded by
`.gitignore` and must be committed only when explicitly approved for distribution.

See `docs/design/002-project-bootstrap-and-distribution.md` for the distribution strategy.
