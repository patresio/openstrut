---
description: Start the SDD workflow to specify a change before implementation
agent: sdd
---

# SDD Workflow Command

Process the following change objective:
$ARGUMENTS

To fulfill this objective, load the `engineering-sdd-change` skill and execute its workflow exactly as written.

Do NOT implement production code.
Do NOT invoke the `build` agent.
You MUST stop at the Approval Gate.
