interface:
  display_name: "Openstrut"
  short_description: "Repo-specific patterns and workflows for openstrut"
  default_prompt: "Use the openstrut repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true